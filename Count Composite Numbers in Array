public class CompositeCount {
    public static void main(String[] args) {

        int arr[] = {4, 7, 8, 9, 11};
        int count = 0;

        for (int num : arr) {
            int factors = 0;

            for (int i = 1; i <= num; i++)
                if (num % i == 0)
                    factors++;

            if (factors > 2)
                count++;
        }

        System.out.println("Composite count: " + count);
    }
}
